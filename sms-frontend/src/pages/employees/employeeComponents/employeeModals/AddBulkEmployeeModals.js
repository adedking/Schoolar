import React from 'react'
import { connect } from 'react-redux'

export const AddBulkEmployeeModals = (props) => {
    return (
        <modal>
            
        </modal>
    )
}

const mapStateToProps = (state) => ({
    
})

const mapDispatchToProps = {
    
}

export default connect(mapStateToProps, mapDispatchToProps)(AddBulkEmployeeModals)
