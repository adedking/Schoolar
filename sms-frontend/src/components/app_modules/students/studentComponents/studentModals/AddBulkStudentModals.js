import React from 'react'
import { connect } from 'react-redux'

export const AddBulkStudentModals = (props) => {
    return (
        <modal>
            
        </modal>
    )
}

const mapStateToProps = (state) => ({
    
})

const mapDispatchToProps = {
    
}

export default connect(mapStateToProps, mapDispatchToProps)(AddBulkStudentModals)
